# gbax

Hacker-first, scriptable GBA emulator. Pip-installable Python package with a CLI
and an HTTP API. Linux-only for now.

See `vault/Atlas/Architecture/2026-06-09-gbax-design.md` for the full design.

## Status

Pre-alpha. Slices 1+2 (basic play + serve) in progress.
